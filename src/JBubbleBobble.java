import VIEW.MainFrame;

/**
 *
 *
 *
 *
 *
 *
 * Classe principale dell'applicazione JB ubbleBobble.
 *
 * Questa classe contiene il metodo `main` che avvia l'applicazione creando e visualizzando
 * la finestra principale dell'interfaccia utente.
 */
public class JBubbleBobble {

    /**
     * Metodo di avvio dell'applicazione.
     *
     * Questo metodo viene eseguito quando l'applicazione viene avviata. Crea un'istanza
     * della classe `MainFrame`, che rappresenta la finestra principale dell'interfaccia utente.
     *
     * @param args Argomenti della riga di comando (non utilizzati in questa applicazione).
     */
    public static void main(String[] args) {
        MainFrame frame = new MainFrame(); // Crea e visualizza la finestra principale dell'applicazione
    }
}


